questions:
    - function(e){ if(e.target.tageName)}??
    - if(event.key === 'Enter'){
        document.execCommand('insertLineBreak');
        event.preventDefault();
    }?????
    - updateStorage --> can you empty storage? how long does the data last in local storage?

LEVEL UP:
    - update local storage into trash bin
    - can delete --> storage for 3 days then delete 